# Splunk Connection Configuration
SPLUNK_BASE_URL=https://localhost:8089
SPLUNK_USERNAME=admin
SPLUNK_PASSWORD=changeme
# Leave empty to use username/password authentication instead of API token
SPLUNK_API_TOKEN=

# Authentication Strategy (session or api-token)
# If SPLUNK_API_TOKEN is set to a non-empty value, it will be used instead of username/password

# Optional: Profile name to load from config file (~/.config/splunk-tui/config.json)
# Profile settings are loaded first, then overridden by environment variables
# SPLUNK_PROFILE=default

# Optional: Skip TLS verification (useful for self-signed certificates)
SPLUNK_SKIP_VERIFY=false

# Optional: Connection timeout in seconds
SPLUNK_TIMEOUT=30

# Optional: Maximum retries for failed requests
SPLUNK_MAX_RETRIES=3

# Logging
RUST_LOG=info
