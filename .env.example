# Splunk Connection Configuration
SPLUNK_BASE_URL=https://localhost:8089
SPLUNK_USERNAME=admin
SPLUNK_PASSWORD=changeme
# Leave empty to use username/password authentication instead of API token
SPLUNK_API_TOKEN=

# Authentication Strategy (session or api-token)
# If SPLUNK_API_TOKEN is set to a non-empty value, it will be used instead of username/password

# Optional: Profile name to load from config file (~/.config/splunk-tui/config.json)
# Profile settings are loaded first, then overridden by environment variables
# SPLUNK_PROFILE=default

# Optional: Skip TLS verification (useful for self-signed certificates)
SPLUNK_SKIP_VERIFY=false

# Optional: Connection timeout in seconds
SPLUNK_TIMEOUT=30

# Optional: Maximum retries for failed requests
SPLUNK_MAX_RETRIES=3

# Optional: Session expiry buffer in seconds (default: 60)
# Proactively refreshes session tokens before they expire to prevent race conditions
# where a token expires during an API call. Increase this if you experience
# session timeout errors during long-running operations.
# SPLUNK_SESSION_EXPIRY_BUFFER=60

# Optional: Session TTL in seconds (default: 3600)
# Controls how long session tokens remain valid before expiring.
# Increase this if your Splunk server has longer session timeouts,
# decrease if you want more frequent re-authentication.
# SPLUNK_SESSION_TTL=3600

# Search Defaults (optional)
# These control the default time range and result limits for searches.
# If not set, defaults are: earliest="-24h", latest="now", max_results=1000
# SPLUNK_EARLIEST_TIME=-24h
# SPLUNK_LATEST_TIME=now
# SPLUNK_MAX_RESULTS=1000

# Logging
RUST_LOG=info
